/** Links into the marketing site (agntchat_website repo). The app sends
 *  people there for the things the site owns — the waitlist, downloads —
 *  so the URLs live in one place. */
export const MARKETING_SITE_URL = "https://agntchat.com";
export const WAITLIST_URL = `${MARKETING_SITE_URL}/waitlist`;
